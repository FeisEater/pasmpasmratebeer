<p id="notice"><%= notice %></p>

<h1>Breweries</h1>

<h2>Active</h2>

<p> Number of active breweries: <%= @active_breweries.count %> </p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Year</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @active_breweries.each do |brewery| %>
      <tr>
        <td><%= link_to brewery.name, brewery %></td>
        <td><%= brewery.year %></td>
        <!--td><%= link_to 'Show', brewery %></td--!>
        <td><%= link_to('Edit', edit_brewery_path(brewery), class: "btn btn-default") if current_user %></td>
        <td><%= link_to('Destroy', brewery, method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' }) if current_user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<h2>Retired</h2>

<p> Number of retired breweries: <%= @retired_breweries.count %> </p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Year</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @retired_breweries.each do |brewery| %>
      <tr>
        <td><%= link_to brewery.name, brewery %></td>
        <td><%= brewery.year %></td>
        <!--td><%= link_to 'Show', brewery %></td--!>
        <td><%= link_to('Edit', edit_brewery_path(brewery), class: "btn btn-default") if current_user %></td>
        <td><%= link_to('Destroy', brewery, method: :delete, class: "btn btn-danger", data: { confirm: 'Are you sure?' }) if current_user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to('New Brewery', new_brewery_path, class:"btn btn-primary") if current_user %>

